name: probe-runners
on:
  workflow_dispatch: {}
jobs:
  probe:
    strategy:
      fail-fast: false
      matrix:
        runner: [macos-13, macos-15-intel]
    runs-on: ${{ matrix.runner }}
    timeout-minutes: 5
    steps:
      - run: uname -a && sw_vers

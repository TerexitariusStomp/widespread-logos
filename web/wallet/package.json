{
  "name": "@widespread/wallet-web",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "license": "MIT OR Apache-2.0",
  "scripts": {
    "build": "esbuild src/main.tsx --bundle --format=esm --outfile=dist/bundle.js --jsx=automatic && cp index.html manifest.webmanifest sw.js dist/ && cp ../../packages/lez-wasm/wsp_lez_wasm_bg.wasm dist/ && cp -r icons dist/"
  },
  "dependencies": {
    "@widespread/wallet-ui": "file:../../packages/wallet-ui",
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "esbuild": "^0.25.0",
    "typescript": "^5.6.0"
  }
}
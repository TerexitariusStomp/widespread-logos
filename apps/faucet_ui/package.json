{
  "name": "@widespread/faucet-ui",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "license": "MIT OR Apache-2.0",
  "scripts": {
    "build": "esbuild src/main.ts --bundle --format=esm --outfile=dist/bundle.js",
    "serve": "python3 -m http.server 8789 -d .."
  },
  "devDependencies": {
    "esbuild": "^0.25.0",
    "typescript": "^5.6.0"
  }
}
